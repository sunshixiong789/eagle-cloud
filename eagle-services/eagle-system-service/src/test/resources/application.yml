spring:
  profiles:
    active: local
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false

eagle:
  jwt:
    keystore-location: classpath:jwt-keystore.p12
    keystore-password: eagle-jwt-dev-2026
    key-alias: eagle-jwt