# -------只适用于开发环境----------
# 网络配置
networks:
  eagle-network:
    driver: bridge

# 数据卷（named volumes，由 docker 自动创建并持久化）
volumes:
  mysql-data:
  redis-data:
  nacos-data:

services:
  # MySQL 数据库
  mysql:
    image: mysql:8.0.30
    container_name: eagle-mysql
    restart: always
    ports:
      - "3306:3306"
    networks:
      - eagle-network
    environment:
      - MYSQL_ROOT_PASSWORD=root123456
      - MYSQL_DATABASE=eagle
      - TZ=Asia/Shanghai
    volumes:
      - mysql-data:/var/lib/mysql
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot123456" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Redis 缓存
  redis:
    image: redis:8.6.1
    container_name: eagle-redis
    restart: always
    ports:
      - "6379:6379"
    networks:
      - eagle-network
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes --requirepass redis123456
    healthcheck:
      test: [ "CMD", "redis-cli", "-a", "redis123456", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # Nacos 注册中心
  nacos:
    image: nacos/nacos-server:v3.2.1
    container_name: eagle-nacos
    restart: always
    ports:
      # Nacos v3 新版控制台容器内端口 8080；为避免与 gateway 主机 8080 冲突，主机映射为 8090
      - "8090:8080"
      - "8848:8848"
      - "9848:9848"
    networks:
      - eagle-network
    environment:
      - PREFER_HOST_MODE=hostname
      - MODE=standalone
      - NACOS_AUTH_IDENTITY_KEY=serverIdentity
      - NACOS_AUTH_IDENTITY_VALUE=security
      - NACOS_AUTH_TOKEN=VGhpc0lzTXlDdXN0b21TZWNyZXRLZXkwMTIzNDU2Nzg=
      - TZ=Asia/Shanghai
    volumes:
      - nacos-data:/home/nacos/data
    healthcheck:
      # Nacos v3.x 把控制台拆成独立 Tomcat：
      #   - 容器内 8080：新版 Console（context path 是 /，无 /nacos 前缀）
      #   - 容器内 8848：Server OpenAPI（保留 /nacos 前缀）
      # 旧的 /nacos/v1/console/health/readiness 在 v3 已移除（启动日志：No mapping for GET ...）；
      # actuator 端点默认未暴露（启动日志：Exposing 0 endpoints beneath base path '/actuator'）。
      # healthcheck 在容器内执行，必须用容器内端口（不是 ports 映射出去的主机端口 8090）。
      # 直接探容器内 8080 的根路径——Tomcat 启动后稳定返回 200，且 standalone 模式下
      # Console 与 Server 同进程同启动，Console 就绪即代表 Nacos 可用。
      test: [ "CMD-SHELL", "curl -fsS -o /dev/null -m 3 http://127.0.0.1:8080/ || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 60s

  # 网关服务
  gateway:
    build:
      context: ./eagle-gateway-service
      dockerfile: Dockerfile
    image: eagle-gateway:1.0.0
    pull_policy: build           # 本地构建镜像，禁止 docker compose up 时去 registry 拉
    container_name: eagle-gateway
    restart: always
    ports:
      - "8080:8080"
    networks:
      - eagle-network
    # .env 用于敏感凭证 / 可调项（OAuth2 回调、第三方 secret 等），见 .env.example
    env_file:
      - .env
    # environment 优先级高于 env_file，写容器网络强约束（即使 .env 误填 localhost 也能兜底）
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - NACOS_SERVER_ADDR=nacos:8848
      - JAVA_OPTS=-server -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    depends_on:
      nacos:
        condition: service_healthy
      system:
        condition: service_healthy

  # 系统服务
  system:
    build:
      context: ./eagle-system-service
      dockerfile: Dockerfile
    image: eagle-system:1.0.0
    pull_policy: build           # 本地构建镜像，禁止 docker compose up 时去 registry 拉
    container_name: eagle-system
    restart: always
    ports:
      - "8082:8082"
    networks:
      - eagle-network
    # .env 提供敏感凭证 / 可调项（admin 密码、JWT keystore 密码、微信 secret 等），见 .env.example
    env_file:
      - .env
    # environment 优先级高于 env_file，强约束 profile 与 nacos 容器内主机名
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - NACOS_SERVER_ADDR=nacos:8848
      - JAVA_OPTS=-server -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8082/actuator/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      nacos:
        condition: service_healthy

